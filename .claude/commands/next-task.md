---
description: Show the next unblocked task from the roadmap
---

Read the roadmap at `roadmap/index.md` and all task files in `roadmap/tasks/`. Find the first task with status `todo` whose dependencies are all `done`. Present it with:

1. Task number and title
2. Phase and priority
3. What it depends on (and confirmation those are done)
4. A brief summary of what needs to happen
5. Which agent or skill should execute it (from `.claude/TEAM.md`)

If multiple tasks are unblocked, list all of them and note which can be parallelized.
