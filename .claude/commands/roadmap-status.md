---
description: Show roadmap progress by phase
---

Read `roadmap/index.md` and summarize progress:

1. For each phase, count tasks by status (todo, in-progress, done)
2. Show overall completion percentage
3. Highlight any blocked tasks (dependencies not met)
4. Show the critical path — what's the longest chain of unfinished dependencies?

Format as a clean table. If $ARGUMENTS specifies a phase number, show detailed task-level status for that phase only.

$ARGUMENTS
